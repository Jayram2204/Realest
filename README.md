
  # Financial Terminal UI Design

  This is a code bundle for Financial Terminal UI Design. The original project is available at https://www.figma.com/design/Knn3Uy4is0AY1uesSS0SwF/Financial-Terminal-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  